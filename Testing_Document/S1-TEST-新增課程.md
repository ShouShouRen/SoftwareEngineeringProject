# **S1-選課系統測試文件：新增課程(Create Course)**

**文件版本：** V1.0  
**文件日期：** 2025/12/04  
**專案作者：** （請填入你的名字）

---

## **1. 進入條件 (Preconditions)**

在執行「新增課程」功能的測試前，需滿足以下條件：

1. **測試環境建置：**  
   - 前後端已部署於測試伺服器 (Staging)  
   - API `/course/create`、`/auth/login` 已可正常使用  

2. **教師帳號：**  
   - 資料庫中已存在至少一組教師帳號  
   - 教師帳號具備「新增課程」權限  

3. **系統狀態：**  
   - 系統可正常登入  
   - 資料庫可正常寫入課程資料  

4. **基礎資料已建立：**  
   - 系統內已有至少一個可選的「上課地點」  
   - 課程命名不得重複（若系統有此限制）

---

## **2. 測試方法 (Test Procedure)**

本功能採用 **黑箱測試 (Black-box Testing)**，測試者不需了解後端邏輯，只需透過 UI 或 API 驗證是否正確。

測試流程包含：

1. **依照測試案例逐步操作 UI 介面或呼叫 API**  
2. **比對畫面與 API 回應是否符合預期結果**  
3. **必要時查詢資料庫 `course` 資料表，確認寫入的欄位是否正確**  
4. **若結果不符，回報為 BUG 並標註嚴重程度**

---

## **3. 測試成功條件 (Success Criteria)**

1. **所有測試案例通過率達 100%**  
2. **無 Critical 或 Major 等級的系統缺陷**  
3. **所有案例覆蓋「新增課程需求規格」中定義的 AC1～AC6**

---

## **4. 新增課程任務拆解 (Task Breakdown)**

### **Task：新增課程 (Create Course)**

| 流程步驟 | 說明 |
|--------|------|
| **T1** | 教師登入系統並通過身份認證 |
| **T2** | 進入「新增課程」頁面 |
| **T3** | 輸入課程基本資料：<br>‧ 課程名稱<br>‧ 上課時間（週別、開始/結束時間）<br>‧ 上課地點<br>‧ 人數上限<br>‧ 選課截止日期 |
| **T4** | 系統驗證輸入資料格式（日期格式、時間區間、上限人數等） |
| **T5** | 驗證課程是否與已存在課程重複（若系統有此規則） |
| **T6** | 儲存課程資料至資料庫 |
| **T7** | 顯示「新增成功」並回到課程列表頁 |

---
## **5. 測試案例 (Test Cases)**

---

### **5.1 測試案例列表**

### **AC1 — 教師必須先登入才能新增課程**

| 測試案例 ID | 案例描述 | 步驟 | 預期結果 |
|-------------|----------|------|-----------|
| **TC-001** | 未登入使用者試圖進入新增課程頁面 | 1. 直接輸入新增課程頁 URL<br>2. 系統檢查登入狀態 | 系統導回登入頁，顯示「請先登入」 |
| **TC-002** | 成功登入後進入新增課程頁 | 1. 教師登入<br>2. 點選「新增課程」 | 成功進入新增課程頁，欄位正常顯示 |

---

### **AC2 — 課程資料格式驗證**

| 測試案例 ID | 案例描述 | 步驟 | 預期結果 |
|-------------|----------|------|-----------|
| **TC-003** | 課程名稱為空 | 1. 進入新增課程頁<br>2. 課程名稱留白<br>3. 點選送出 | 顯示錯誤訊息「課程名稱不可空白」，不能送出 |
| **TC-004** | 上課時間結束早於開始 | 1. 選擇開始時間 14:00<br>2. 結束時間 13:00 | 顯示錯誤「結束時間需晚於開始時間」 |
| **TC-005** | 人數上限輸入負數 | 1. 輸入上限為 -5<br>2. 點擊送出 | 顯示「人數上限必須大於 0」 |

---

### **AC3 — 上課地點選擇與驗證**

| 測試案例 ID | 案例描述 | 步驟 | 預期結果 |
|-------------|----------|------|-----------|
| **TC-006** | 選擇有效地點 | 1. 上課地點下拉選單選擇「E-201」<br>2. 點擊送出 | 後端成功接收地點 ID，資料成功儲存 |
| **TC-007** | 地點未選取 | 1. 不選地點直接送出 | 顯示錯誤「請選擇上課地點」 |

---

### **AC4 — 選課截止日期驗證**

| 測試案例 ID | 案例描述 | 步驟 | 預期結果 |
|-------------|----------|------|-----------|
| **TC-008** | 截止日期早於今日 | 1. 選擇截止日＝昨日<br>2. 點送出 | 顯示「截止日期不可早於今日」 |
| **TC-009** | 正確截止日期 | 1. 選擇合理的截止日<br>2. 點送出 | 成功通過驗證 |

---

### **AC5 — 課程資料寫入資料庫**

| 測試案例 ID | 描述 | 步驟 | 預期結果 |
|-------------|------|------|-----------|
| **TC-010** | 成功建立課程 | 1. 填入所有正確資料<br>2. 點送出<br>3. 查詢資料庫 | 新增課程出現在 DB：課程名稱、時間、地點、人數上限等欄位正確 |
| **TC-011** | 後端寫入失敗（模擬 DB error） | 1. API 模擬 500<br>2. 點送出 | 顯示「新增失敗，請稍後再試」，不應寫入資料庫 |

---

### **AC6 — 建立成功後顯示結果**

| 測試案例 ID | 描述 | 步驟 | 預期結果 |
|-------------|------|------|-----------|
| **TC-012** | 新增成功後 UI 顯示確認訊息 | 1. 填寫正確資料<br>2. 成功建立課程 | 顯示訊息「課程已成功建立」，並回到課程列表 |
| **TC-013** | 課程列表顯示新課程 | 1. 建立課程<br>2. 返回課程列表 | 新課程正確顯示於課程列表，資訊完整 |

---


